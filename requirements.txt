# Agent Scheduler — runtime dependencies (Python 3.12)
# Pinned for reproducibility between host venv and container.

fastapi==0.121.1         # admin API
uvicorn==0.49.0          # ASGI server (matches agent_bus)
taskiq-redis==1.2.3      # broker + schedule source (Redis/valkey); pulls taskiq core
taskiq==0.12.4           # scheduler engine: per-SECOND wall-clock re-anchored loop
redis>=8,<9              # redis-py: taskiq-redis backend (needs >=8) + our registry/dedupe
croniter==3.0.3          # compute next_run_time for the admin view
valkey-glide==2.4.2      # bus event emission — pinned to match agent_bus wire client
pydantic==2.13.4         # models + vendored EventEnvelope (match agent_bus)
python-dotenv==1.2.2     # .env loading for local/dev

# dev/test
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1            # FastAPI TestClient
