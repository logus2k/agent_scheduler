"""DEPRECATED — removed in the Taskiq migration.

APScheduler + RedisJobStore was replaced by Taskiq (see tasks.py: broker,
RegistryScheduleSource, scheduler). This module is intentionally empty and
kept only as a tombstone; delete it once the change is committed.
"""
